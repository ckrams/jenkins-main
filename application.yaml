apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jenkins-app
  namespace: argocd
spec:
  project: jenkins

  source:
    repoURL: https://github.com/ckrams/jenkins-main.git
    targetRevision: HEAD
    path: jenkins
  destination: 
    server: https://kubernetes.default.svc
    namespace: jenkins

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
